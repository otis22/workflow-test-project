# PRD: 2.1 Сущность User

## Цель

Создать доменную модель пользователя в `app/Domain/User/`.

## Из доменной модели (artifacts/domain-model-taskflow-ru.md)

Поля: id, name, email, password_hash, created_at, updated_at.

## Декомпозиция

### Подзадача 2.1.1: Создать доменную сущность User

- `app/Domain/User/User.php` — доменная сущность (не Eloquent model)
- Валидация: name не пустое, email валидный
- Value object для Email

### Подзадача 2.1.2: Тесты

- Unit тесты для создания User, валидации полей
