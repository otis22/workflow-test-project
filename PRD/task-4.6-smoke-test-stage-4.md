# PRD: Task 4.6 Smoke test stage 4

## Goal

Verify that the completed task-management stage works on the running Docker Compose stack and that the main task workflows respond correctly in the live application.

## Acceptance Criteria

- Docker Compose starts the application stack without errors.
- The main stage URLs return the expected responses in the running environment.
- The delivered task workflows are revalidated against the running stack.
- Smoke-test results are recorded in `AssumptionLog.md`.
